Prototype static dashboard

Open `index.html` in a browser (or serve with a simple HTTP server) to preview explainability images placed under `outputs/explanations`.

Quick local server (Python):

```bash
# from repository root
python -m http.server --directory frontend/prototype 8000
# then open http://localhost:8000
```
